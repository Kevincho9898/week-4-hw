function setup() {
  createCanvas(400, 400);
  colorMode(HSB);
}

function draw() {
  background(255);

x
  for (var x = 0; x < width-10; x = x + 10) {
    line(x, height/2, x+100, height/2-75,stroke(random(10,290),110,123) );
  }
}
